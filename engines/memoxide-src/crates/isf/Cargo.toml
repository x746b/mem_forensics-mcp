[package]
name = "isf"
version = "0.1.0"
edition = "2021"
description = "Parser for Volatility3 Intermediate Symbol Format (ISF) files"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
xz2 = "0.1"
thiserror = "1.0"
